// @flow

import type { StatusType } from 'types/statusTypes';

export const STATUS_DEFAULT: StatusType = 'default';
export const STATUS_LOADING: StatusType = 'loading';
export const STATUS_SUCCESS: StatusType = 'success';
export const STATUS_FAILURE: StatusType = 'fail';
export const STATUS_RUNNING: StatusType = 'running';

export default {
  STATUS_DEFAULT,
  STATUS_LOADING,
  STATUS_SUCCESS,
  STATUS_FAILURE,
  STATUS_RUNNING,
};
